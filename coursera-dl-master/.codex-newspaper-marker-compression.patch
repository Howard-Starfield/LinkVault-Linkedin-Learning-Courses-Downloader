diff --git a/apps/desktop/src-tauri/src/newspaper/models.rs b/apps/desktop/src-tauri/src/newspaper/models.rs
index 9d4be44..0000000 100644
--- a/apps/desktop/src-tauri/src/newspaper/models.rs
+++ b/apps/desktop/src-tauri/src/newspaper/models.rs
@@ -106,6 +106,10 @@ pub fn expand_dates(
         .collect())
 }
 
+pub const fn default_optimization_quality() -> u8 {
+    86
+}
+
 #[derive(Debug, Clone, Serialize, Deserialize)]
 #[serde(rename_all = "camelCase")]
 pub struct CreateNewspaperBatchRequest {
@@ -118,6 +122,8 @@ pub struct CreateNewspaperBatchRequest {
     pub delay_seconds: u32,
     pub optimize_images: bool,
     pub optimization_profile: String,
+    #[serde(default = "default_optimization_quality")]
+    pub optimization_quality: u8,
     pub keep_original_jpg: bool,
 }
 
@@ -130,6 +136,7 @@ pub struct NewspaperBatch {
     pub delay_seconds: u32,
     pub optimize_images: bool,
     pub optimization_profile: String,
+    pub optimization_quality: u8,
     pub keep_original_jpg: bool,
     pub created_at: i64,
     pub updated_at: i64,
@@ -163,6 +170,7 @@ pub struct NewspaperSchedule {
     pub delay_seconds: u32,
     pub optimize_images: bool,
     pub optimization_profile: String,
+    pub optimization_quality: u8,
     pub keep_original_jpg: bool,
     pub last_run_date: Option<String>,
     pub last_error: Option<String>,
@@ -179,6 +187,8 @@ pub struct CreateNewspaperScheduleRequest {
     pub delay_seconds: u32,
     pub optimize_images: bool,
     pub optimization_profile: String,
+    #[serde(default = "default_optimization_quality")]
+    pub optimization_quality: u8,
     pub keep_original_jpg: bool,
 }
diff --git a/apps/desktop/src-tauri/src/newspaper/optimizer.rs b/apps/desktop/src-tauri/src/newspaper/optimizer.rs
index 7f98267..0000000 100644
--- a/apps/desktop/src-tauri/src/newspaper/optimizer.rs
+++ b/apps/desktop/src-tauri/src/newspaper/optimizer.rs
@@ -15,8 +15,8 @@ pub enum OptimizationError {
     #[error("optimized image dimensions changed")]
     DimensionMismatch,
-    #[error("unsupported optimization profile: {0}")]
-    UnsupportedProfile(String),
+    #[error("WebP quality must be between 55 and 95, received {0}")]
+    UnsupportedQuality(u8),
     #[error("WebP encoder could not accept this image")]
     Encoder,
 }
@@ -24,14 +24,11 @@ pub enum OptimizationError {
 pub fn optimize_page(
     source: &Path,
-    profile: &str,
+    quality: u8,
 ) -> Result<OptimizationOutcome, OptimizationError> {
-    let quality = match profile {
-        "webp_high" => 92.0,
-        "webp_balanced" => 86.0,
-        other => return Err(OptimizationError::UnsupportedProfile(other.to_string())),
-    };
+    if !(55..=95).contains(&quality) {
+        return Err(OptimizationError::UnsupportedQuality(quality));
+    }
     let source_bytes = std::fs::read(source)?;
     let image = image::load_from_memory(&source_bytes)?;
     let dimensions = image.dimensions();
     let rgba = image.to_rgba8();
     let encoded =
-        webp::Encoder::from_rgba(rgba.as_raw(), dimensions.0, dimensions.1).encode(quality);
+        webp::Encoder::from_rgba(rgba.as_raw(), dimensions.0, dimensions.1)
+            .encode(f32::from(quality));
@@ -76,7 +73,7 @@ mod tests {
             .save_with_format(&source, image::ImageFormat::Jpeg)
             .unwrap();
 
-        let outcome = optimize_page(&source, "webp_high").unwrap();
+        let outcome = optimize_page(&source, 92).unwrap();
         match outcome {
             OptimizationOutcome::Replaced { path, .. } => {
                 assert_eq!(image::open(path).unwrap().dimensions(), (480, 640));
@@ -89,12 +86,12 @@ mod tests {
     }
 
     #[test]
-    fn unsupported_profile_never_changes_the_source() {
+    fn unsupported_quality_never_changes_the_source() {
         let directory = tempdir().unwrap();
         let source = directory.path().join("A01.jpg");
         std::fs::write(&source, b"source").unwrap();
         assert!(matches!(
-            optimize_page(&source, "lossless"),
-            Err(OptimizationError::UnsupportedProfile(_))
+            optimize_page(&source, 54),
+            Err(OptimizationError::UnsupportedQuality(54))
         ));
         assert_eq!(std::fs::read(source).unwrap(), b"source");
     }
